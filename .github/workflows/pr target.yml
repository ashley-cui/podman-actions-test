name: "pr target"
on:
  pull_request_target:
    types:
      - opened
      - edited

jobs:
  triage:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - name: Run "tests"
        run: |
          echo "pr target"
