# https://github.com/actions/labeler
name: "release pr"
on:
  pull_request_target:
    paths:
    - 'version/rawversion/version.go'
jobs:
  triage:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - name: wheeeee
      run: echo "runrunrun"
